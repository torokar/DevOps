## Инициализация репозитория

git init

Команда используется для создания нового репозитория Git. Она инициализирует пустой репозиторий
в указанной директории, создавая все необходимые файлы и директории для работы с Git

---
## Настройка параметров конфигурации Git 

git config

Команда, используемая для настройки параметров конфигурации Git. Эти параметры могут 
быть настроены для текущего пользователя, всего репозитория или глобально (флаг --global) 
для всех репозиториев на компьютере

Например:
git config user.name "Ivan"

Если хотите указать имя также и для будущих проектов, то используйте флан --global

git config --global user.name "Ivan"

Тоже самое можно проделать и с почтой. Она также будет отображатся в дальнейших коммитах

git config user.email "your@mail.ru"
	или глобально 
git config --global user.email "your@mail.ru"
---
## Состояние рабочего каталога и индекса 

git status 

Команда используется для отображения состояния рабочего каталога и индекса. Она показывает 
изменения, которые были добавлены в индекс и готовы для коммита, а также изменения, которые 
еще не были добавлены. 
---
## Добавление изменений в индекс

git add

Команда используется для добавления изменений в рабочем каталоге в индекс, полготавливая 
их для следующего коммита. Это первый шаг в процессе сохранения изменений в git

---
## Отмена изменений в репозитории

git reset

Команда используется для отмены изменений в репозитории. Она может изменять как индекс, так 
и рабочий каталог. **git reset** имеет несколько режимов, которые определяют степень отката
изменений *--sorf*, **--mixed** и **--hard**

**--soft** - оставляет изменения в рабочем каталоге и в индексе. Перемещает указатель текущей
ветки к указанному коммиту

**--mixed** - (по умолчанию) Оставляет изменения в рабочем каталоге, но удаляет их из 
индекса

**--hard** - удаляет изменения из рабочего каталога и индекса, возвращает состояние файлов к
указанному коммиту

!!! Будте аккуратны, когда используете флаг **--hard** !!!

**--soft HEAD~1** - откатить коммит, оставить изменения в индексе

**git reset --keep HEAD~1** откатить сохранив локальные изменения

Если хотите удалить какой либо файл (допустим index.js) из индекса пропишите 

```
git reset index.js
```

После чего он снова станет неотслеживаемым
Если нужно удалить все файлы из индекса, просто пропишите git reset



**git reset --hard HEAD~1** - удаляет ранее добавленный файл его коммит и индекс

**git reset --soft HEAD~1** - удаляет коммит, но оставляет в отслеживающимся файл




---
## Сохранение изменений в локальном репозитории Git

git commit

Команда используется для сохранения изменений в локальном репозитории Git. Когда вы выполняете
git commit, Git сохраняет текущие изменения, добавленные в индекс, в виде коммита с уникальным
индефикатором, который содержитинформацию о всех изменениях, авторе, времени и сообщений 
коммита

Создадим commit

git commit -m "Initial commit"

Предположим, что мы забыли добавить index.css Для начала создадим его, затем добавим его 
в индекс, использую команды
```
echo "* {margib: 0; }" > index.css
git add index.css
git commit --amend --no-edit
```

Основные команды:


```
git commit --amend --no-edit - изменить коммит без изменения его сообщения

git commit -a -m - обновил все измененные файлы - добавление всех измененных файлов и 
создания коммита

git commit --author="Ivan <ivan@example.com>" -m "Добавил новые функции" 

дыи создание коммита с
указанием автора

git commit --allow-empty -m "Пустой коммит" - создание пустого коммита (без изменений)
```

---
## Просмотр истории коммитов

git log

*Основные команды*

```
git log --oneline - компактный вывод

git log -p - просмотр коммитов с определенными изменениями 

git log -n - количество выводимых коммитов

git log --graph - вывод изменений в виде дерева

git log --stat - вывод статистики изменений

git log --author="Ivan" - фильтрация коммитов по автору

git log --since="2024-22-07" (c) и --until="2024-22-08" (до) - фильтрация коммитов по дате

git log index.html" - показывает коммиты, затрагивающие конкретный файл

git log --name-only - показывает только имена измененных файлов  

```

---
## Отправка коммитов в удаленный репозиторий

git push


Здесь нам как раз-таки и понадобится та самая преустановка, которую мы провели в самом
начале.

Нужно скопировать строку с 

```https://github.com/<Ваш аккаунт>/<Ваш репозиторий>.git```

и добавить удаленный репозиторий 

``` git remote add origin https://github.com/<Ваш аккаунт>/<Ваш репозиторий>.git ```

Вместо *origin* можем дать свое название, но по умолчанию обычно используется *origin*

Если снова попробуем ввести команду *git push* то получим

```
fatal: The current branch master has no upstream branch.
To push the current branch and set the remote as upstream, use

    git push --set-upstream origin master

To have this happen automatically for branches without a tracking
upstream, see 'push.autoSetupRemote' in 'git help config'.
```

Сообщение, которое вы получили, означает, что ваша текущая ветка master не связана с 
удаленной вектой в вашем удаленном рерозитории(origin). Upstream ветка - это удаленная 
ветка, с которой ваша локальная ветка синхронизируется при выполнении команд *git push* и
*git pull*

Если вы хотите, чтобы Git автоматически устанавливал upstream ветку при первом пуше новой
локальной ветки, вы можете настроить параметр push.autoSetupRemote. Для этого выполните 
команду 

``` git config --global push.autoSetupRemote true```

---  






